public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# Use public registry by default (overrides global/user config if present)
registry=https://registry.npmjs.org/

# Disable any globally-configured proxy (common in corporate networks)
proxy=
https-proxy=
